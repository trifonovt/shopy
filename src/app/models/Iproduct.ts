
export interface IProduct {
  id?: number;
  category?: string;
  title?: string;
  description?: string;
  price?: number;
  imgUrl?: string;
  // save();
  // delete();
  // update();
  // get();
}
